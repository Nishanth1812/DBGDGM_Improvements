# DigitalOcean / AMD GPU dependencies for MM-DBGDGM local training.
#
# Install the matching ROCm PyTorch wheels first, then install the remaining
# Python packages from this file. The exact ROCm index URL depends on the
# droplet image and driver stack. Example:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/rocm6.2
#
# This file intentionally omits torch/torchvision so it will not overwrite a
# ROCm-enabled install.

nilearn>=0.10.0
nibabel>=5.0.0
pydicom>=2.4.0
networkx>=3.2
numpy>=1.26
scipy>=1.12
scikit-learn>=1.4
pandas>=2.0
pyyaml>=6.0
matplotlib>=3.8
tensorboard>=2.15
tqdm>=4.66
opencv-python-headless>=4.9
